services:
  - type: web
    name: custom-auth-panel
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: ADMIN_PASSWORD
        sync: false  # Set in Render dashboard
